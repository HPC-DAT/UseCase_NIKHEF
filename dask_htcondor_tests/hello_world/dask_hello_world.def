Bootstrap: docker
From: python:3.12-slim

%post
    pip install --no-cache-dir \
        dask[distributed] \
        dask-jobqueue \
        numpy

%labels
    Description Dask HTCondor hello-world worker image
